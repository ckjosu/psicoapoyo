# ============================================================
# PsicoApoyo — Variables de entorno
# Copiar como .env y cambiar las contraseñas antes de usar
# cp .env.example .env
# ============================================================

# ── Credenciales MySQL (imagen mysql:8.0) ──
MYSQL_ROOT_PASSWORD=cambia_esto_root
MYSQL_DATABASE=psicoapoyo
MYSQL_USER=psico_user
MYSQL_PASSWORD=cambia_esto_password

# ── Credenciales PHP/PDO (api.php vía getenv()) ──
# IMPORTANTE: DB_HOST=db (nombre del servicio Docker, NO localhost)
DB_HOST=db
DB_NAME=psicoapoyo
DB_USER=psico_user
DB_PASS=cambia_esto_password
